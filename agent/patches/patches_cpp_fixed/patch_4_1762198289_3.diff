diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -13,9 +13,9 @@ Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, std::vector<uint8_t> img) {
     // Initialize member variables in the constructor
     bf = newBf;
     bi = newBi;
     imgData = img;
-}
+    // Use initialization lists for better performance
+    bf = newBf();
+    bi = newBi();
+    imgData = img;
    imgData.assign(newImgData.begin(), newImgData.end());