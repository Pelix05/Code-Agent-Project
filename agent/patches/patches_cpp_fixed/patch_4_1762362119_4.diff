diff --git a/bmp/bmp.cpp b/bmp/bmp.cpp
--- a/bmp/bmp.cpp
+++ b/bmp/bmp.cpp
@@ -11,11 +11,11 @@
 {
-Bmp::Bmp(BmpFileHeader &newBf, BmpInfoHeader &newBi, std::vector<uint8_t> img)
+// Change the constructor to initialize variables in the initialization list
+Bmp::Bmp(BmpFileHeader newBf, BmpInfoHeader newBi, const std::vector<uint8_t>& img)
    bf = newBf;
    bi = newBi;
    imgData = img;
diff --git a/bmpfileheader.cpp b/bmpfileheader.cpp
--- a/bmpfileheader.cpp
+++ b/bmpfileheader.cpp
@@ -3,16 +3,16 @@
 BmpFileHeader::BmpFileHeader()
 {
   // Initialize member variables with their values from the constructor arguments
   bfType = newBf.getBFType();
   bfSize = newBf.getBFSize();
   bfReserved1 = newBf.getBFReserved1();
   bfReserved2 = newBf.getBFReserved2();
   bfOffBits = newBf.getBFOffBits();
   // Rest of the uninitMemberVars warnings remain unchanged
diff --git a/database.cpp b/database.cpp
@@ -5,5 +5,5 @@
 m_db = QSqlDatabase::addDatabase("QSQLITE");
diff --git a/defaultpuzzlemenuwindow.h b/defaultpuzzlemenuwindow.h
diff --git a/generatepuzzlemenuwindow.h b/generatepuzzlemenuwindow.h
diff --git a/mainwindowpuzzle.h b/mainwindowpuzzle.h
diff --git a/puzzle/database.cpp b/puzzle/database.cpp
@@ -5,5 +5,5 @@
 m_db = QSqlDatabase::addDatabase("QSQLITE");
diff --git a/databasemanager.h b/databasemanager.h
--- a/databasemanager.h
+++ b/databasemanager.h
@@ -1,9 +1,9 @@
 class DatabaseManager
 {
-    DatabaseManager(); // Removed one-definition rule violation
+    DatabaseManager(const QString& dbUrl); // Initialize with database URL
    // ... rest of the class definition
 };
diff --git a/leaderboard.h b/leaderboard.h
@@ -1,9 +1,9 @@
 class Leaderboard : public QWidget
 {
-    Leaderboard(); // Removed one-definition rule violation
+    Leaderboard(const QString& leaderboardName); // Initialize with leaderboard name
    // ... rest of the class definition
 };