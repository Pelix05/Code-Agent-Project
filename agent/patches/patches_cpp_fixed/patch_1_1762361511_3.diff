diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -13,7 +13,7 @@ Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, const std::vector<uint8_t>& img)
     // Initialize variables in the constructor
     bf = newBf;
     bi = newBi;
-    imgData = img; // Change to use initialization list
+    imgData = std::move(img); // Use move semantics for better performance
 void Bmp::setImgData(const std::vector<uint8_t>& newImgData) {
     imgData = newImgData;