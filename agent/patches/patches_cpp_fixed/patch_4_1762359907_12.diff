diff --git a/bmp/bmpinfoheader.cpp b/bmp/bmpinfoheader.cpp
--- a/bmp/bmpinfoheader.cpp
+++ b/bmp/bmpinfoheader.cpp
@@ -3,7 +3,7 @@
 #include "bmpinfoheader.h"
-BmpInfoHeader::BmpInfoHeader() {
+BmpInfoHeader::BmpInfoHeader(int NZwidth, int height) : biWidth(NZwidth), biHeight(height) {}
 BmpInfoHeader::BmpInfoHeader(int biWidth, int biHeight) :
     biSize(0), biWidth(biWidth), biHeight(biHeight) {
     // Initialize member variables in the constructor
     biPlanes(1), biBitCount(24), biCompression(0), biSizeImage(0);
     // ... (other uninitMemberVar initialization)
 }