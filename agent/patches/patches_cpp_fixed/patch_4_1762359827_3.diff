diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -13,7 +13,7 @@ Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, std::vector<uint8_t> img)
         bf = newBf;
         bi = newBi;
         imgData = img;
-        // Initialize variables in initialization lists
+        bf = newBf; // Use initialization list for bf
+        bi = newBi; // Use initialization list for bi
     }
 void Bmp::setImgData(const std::vector<uint8_t>& newImgData) {
     imgData = newImgData;