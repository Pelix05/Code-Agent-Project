diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -12,7 +12,7 @@ Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, std::vector<uint8_t> img)
         bi = newBi;
         imgData = img;
     }
-    // Remove the assignments in the constructor body
+    bf = newBf; // Use initialization list for bf
+    bi = newBi; // Use initialization list for bi
     imgData = img; // Use initialization list for imgData
 void Bmp::setImgData(const std::vector<uint8_t>& newImgData) {
     // Rest of the function remains unchanged