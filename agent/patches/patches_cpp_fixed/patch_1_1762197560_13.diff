diff --git a/bmp/bmpinfoheader.cpp b/bmp/bmpinfoheader.cpp
--- a/bmp/bmpinfoheader.cpp
+++ b/bmp/bmpinfoheader.cpp
@@ -3,9 +3,11 @@
-BmpInfoHeader::BmpInfoHeader() {}
+BmpInfoHeader::BmpInfoHeader(int NZwidth, int height) : biWidth(NZwidth), biHeight(height) {}
-BmpInfoHeader::BmpInfoHeader() : biWidth(0), biHeight(0) {} // Removed unnecessary assignments in constructor
diff --git a/database.cpp b/database.cpp
--- a/database.cpp
+++ b/database.cpp
@@ -5,5 +5,6 @@ BmpDatabase::BmpDatabase() {
     m_db = QSqlDatabase::addDatabase("QSQLITE");
 }
+BmpDatabase::BmpDatabase() : m_db(nullptr) // Initialize m_db in the constructor
    if (!m_db.open()) {
        // Handle database connection error
    }
diff --git a/defaultpuzzlemenuwindow.h b/defaultpuzzlemenuwindow.h
--- a/defaultpuzzlemenuwindow.h
+++ b/defaultpuzzlemenuwindow.h
@@ -9,9 +9,10 @@ private:
     Q_OBJECT
     // Remove the unknown macros error
-    private slots:
+    private:
         // ... (other slots)
diff --git a/generatepuzzlemenuwindow.h b/generatepuzzlemenuwindow.h
--- a/generatepuzzlemenuwindow.h
+++ b/generatepuzzlemenuwindow.h
@@ -9,9 +9,10 @@ private:
     Q_OBJECT
     // Remove the unknown macros error
-    private slots:
+    private:
         // ... (other slots)
diff --git a/mainwindowpuzzle.h b/mainwindowpuzzle.h
--- a/mainwindowpuzzle.h
+++ b/mainwindowpuzzle.h
@@ -60,9 +60,10 @@ private:
     Q_OBJECT
     // Remove the unknown macros error
-    private slots:
+    private:
         // ... (other slots)
diff --git a/puzzle/databasemanager.h b/puzzle/databasemanager.h
--- a/puzzle/databasemanager.h
+++ b/puzzle/databasemanager.h
@@ -1,2 +1,3 @@
-class DatabaseManager {
+class DatabaseManager : public QObject
 {
     Q_OBJECT
diff --git a/leaderboard.h b/leaderboard.h
--- a/leaderboard.h
+++ b/leaderboard.h
@@ -1,9 +1,10 @@
-class Leaderboard : public QWidget
+class Leaderboard : public QWidget
 {
     Q_OBJECT
diff --git a/puzzle.cpp b/puzzle.cpp
--- a/puzzle.cpp
+++ b/puzzle.cpp
@@ -5,5 +5,6 @@ BmpDatabase& Puzzle::getDatabase() {
     return m_db;
 }
+BmpDatabase& Puzzle::getDatabase() {
+    if (!m_db) {
+        m_db = QSqlDatabase::addDatabase("QSQLITE");
+    }
+    return m_db;