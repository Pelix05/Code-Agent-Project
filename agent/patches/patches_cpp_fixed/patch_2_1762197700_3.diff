diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -14,7 +14,7 @@ Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, std::vector<uint8_t> img) {
     imgData = img;
 }
-// Change initialization lists to avoid constructor body assignments
+void Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, std::vector<uint8_t> img) {
     bf = newBf; // Move initialization to initialization list
     bi = newBi; // Move initialization to initialization list
     imgData = img; // Initialization remains in place
 }
    // No change needed for this function