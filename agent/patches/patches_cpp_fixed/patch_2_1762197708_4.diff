diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -11,7 +11,7 @@ namespace bmp {
   Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, std::vector<uint8_t> img)
   {
     // Initialize variables in the initialization list
     bf = newBf; // [useInitializationList]
-    bi = newBi;
+    bi = BmpInfoHeader(NZwidth, height); // [useInitializationList]
     imgData = img;
   }
   // Rest of the code...