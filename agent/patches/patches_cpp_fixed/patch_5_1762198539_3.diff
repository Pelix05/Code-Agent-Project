diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -13,9 +13,9 @@ Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, const std::vector<uint8_t>& img) {
     bf = newBf;
     bi = newBi;
-    imgData = img; // Change to initialization list
+    imgData = std::move(img); // Use std::move for moved initialization
 }
    imgData = newImgData;