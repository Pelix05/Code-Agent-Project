diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -13,9 +13,9 @@ Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, const std::vector<uint8_t>& img)
     bf = newBf; // Use initialization list for better performance
     bi = newBi; // Use initialization list for better performance
     imgData = img; // Use initialization list for better performance
-    // Remove unnecessary assignment of imgData
+    imgData = std::move(img); // Initialize imgData using move constructor for better performance
 }
    imgData.assign(newImgData.begin(), newImgData.end()); // No need to assign again, as it's already initialized