diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -12,7 +12,7 @@
     BmpFileHeader newBf(NZwidth, height);
     BmpInfoHeader newBi(NZwidth, height);
-    bf = newBf;
-    bi = newBi;
-    imgData = img;
+    bf = newBf;
+    bi = newBi;
     // Initialize imgData in the constructor using const reference for better performance
     imgData = std::move(img);
diff --git a/bmpfileheader.cpp b/bmpfileheader.cpp
--- a/bmpfileheader.cpp
+++ b/bmpfileheader.cpp
@@ -3,16 +3,16 @@
     BmpFileHeader() {
         // Initialize member variables in the constructor
         bfType = 0; // Add missing initialization
         bfSize = 0;
         bfReserved1 = 0;
         bfReserved2 = 0;
         bfOffBits = 0;
+       // Initialize remaining uninitialized member variables
+       bfType = static_cast<BmpFileHeader::BFType>(newBf.getBfType());
+       bfSize = newBf.getBfSize();
+       bfReserved1 = newBf.getBfReserved1();
+       bfReserved2 = newBf.getBfReserved2();
+       bfOffBits = newBf.getBfOffBits();
     }
diff --git a/database.cpp b/database.cpp
--- a/database.cpp
+++ b/database.cpp
@@ -5,5 +5,5 @@
     m_db = QSqlDatabase::addDatabase("QSQLITE");
-    // Initialize m_db in the constructor
-    m_db = QSqlDatabase::addDatabase("QSQLITE");
+    // Initialize m_db in the constructor using initialization list
+    m_db = QSqlDatabase::addDatabase("QSQLITE", /* Additional configuration */);