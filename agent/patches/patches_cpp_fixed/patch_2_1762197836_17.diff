diff --git a/bmp/bmpinfoheader.cpp b/bmp/bmpinfoheader.cpp
--- a/bmp/bmpinfoheader.cpp
+++ b/bmp/bmpinfoheader.cpp
@@ -3,16 +3,16 @@
-BmpInfoHeader::BmpInfoHeader() {
-    // Uninitialized member variables
+BmpInfoHeader::BmpInfoHeader(const BmpFileHeader& newBf, int NZwidth, int height) {
+    bfType = newBf.bfType;
+    bfSize = newBf.bfSize;
+    bfReserved1 = newBf.bfReserved1;
+    bfReserved2 = newBf.bfReserved2;
+    bfOffBits = newBf.bfOffBits;
+    biSize = newBi.biSize;
+    biWidth = NZwidth;
+    biHeight = height;
+    biPlanes = 1; // Assuming grayscale, adjust for other planes if needed
+    biBitCount = newBi.biBitCount;
+    biCompression = BI_RGB; // Default compression for uncompressed image
+    biSizeImage = biWidth * biHeight * (newBi.biBitCount / 8);
+    biXPelsPerMeter = 0;
+    biYPelsPerMeter = 0;
+    biClrUsed = 0;
+    biClrImportant = 0;
+}