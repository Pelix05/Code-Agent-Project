diff --git a/bmpfileheader.cpp b/bmpfileheader.cpp
--- a/bmpfileheader.cpp
+++ b/bmpfileheader.cpp
@@ -3,11 +3,11 @@
-BmpFileHeader::BmpFileHeader() {
+BmpFileHeader::BmpFileHeader(int NZwidth, int height) : bfType(0), bfSize(0), bfReserved1(0), bfReserved2(0), bfOffBits(0) {
   // Initialize member variables in the constructor
   this->bfSize = newBf.bfSize; // Use initialization list for bfSize
   this->bfWidth = NZwidth; // Assign directly, as width is not dependent on constructor arguments
   this->bfHeight = height; // Assign directly, as height is not dependent on constructor arguments
-BmpFileHeader::~BmpFileHeader() {}
+// Rest of the class implementation remains unchanged, but add necessary destructor if needed