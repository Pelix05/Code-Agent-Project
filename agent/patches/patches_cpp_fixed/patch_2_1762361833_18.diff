diff --git a/bmp/bmpinfoheader.cpp b/bmp/bmpinfoheader.cpp
--- a/bmp/bmpinfoheader.cpp
+++ b/bmp/bmpinfoheader.cpp
@@ -3,12 +3,12 @@
 #include "bmpinfoheader.h"
-BmpInfoHeader::BmpInfoHeader() {
+BmpInfoHeader::BmpInfoHeader(int NZwidth, int height) : biWidth(NZwidth), biHeight(height) {
   // Rest of the constructor body, skipping uninitialized member var initialization
 }
-BmpInfoHeader::BmpInfoHeader(BmpFileHeader& newBf, std::vector<uint8_t> img) {
+// Change this line to pass 'img' by const reference
+BmpInfoHeader::BmpInfoHeader(BmpFileHeader& newBf, const std::vector<uint8_t>& img) {
     // ... rest of the constructor
 }