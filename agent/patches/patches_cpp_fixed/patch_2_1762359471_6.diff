diff --git a/bmp/bmpfileheader.cpp b/bmp/bmpfileheader.cpp
--- a/bmp/bmpfileheader.cpp
+++ b/bmp/bmpfileheader.cpp
@@ -3,7 +3,7 @@
 #include "bmpfileheader.h"
-BmpFileHeader::BmpFileHeader() {
+BmpFileHeader::BmpFileHeader(const BmpFileHeader& newBf) : bfType(newBf.bfType), bfSize(newBf.bfSize), bfReserved1(newBf.bfReserved1), bfReserved2(newBf.bfReserved2), bfOffBits(newBf.bfOffBits) {}
 BmpFileHeader(int NZwidth, int height) {
     // Move assignments to initialization list to improve performance
     bfType = NEW_BF_TYPE; // Replace with actual value
     bfSize = NZwidth * height * 3; // Assuming 3 bytes per pixel
     // ... (rest of the constructor)