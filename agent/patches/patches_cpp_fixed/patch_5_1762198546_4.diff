diff --git a/bmp/bmp.cpp b/bmp/bmp.cpp
--- a/bmp/bmp.cpp
+++ b/bmp/bmp.cpp
@@ -10,7 +10,7 @@ Bmp::Bmp(BmpFileHeader& newBf, BmpInfoHeader& newBi, std::vector<uint8_t> img)
     // BmpFileHeader bf(NZwidth, height);
     // BmpInfoHeader bi(NZwidth, height);
     bf = newBf; // Use initialization list for performance improvement
     bi = newBi;
     imgData = img; // Initialize imgData in initialization list