diff --git a/bmp/bmp.cpp b/bmp/bmp.cpp
--- a/bmp/bmp.cpp
+++ b/bmp/bmp.cpp
@@ -12,7 +12,7 @@ Bmp::Bmp(BmpFileHeader &newBf, BmpInfoHeader &newBi, std::vector<uint8_t> img)
     bi = newBi;
     imgData = img;
 }
-Bmp::Bmp(BmpFileHeader newBf, BmpInfoHeader newBi, std::vector<uint8_t> img) {
+// Fixed constructor to initialize variables in initialization lists
+Bmp::Bmp(BmpFileHeader newBf, BmpInfoHeader newBi, const std::vector<uint8_t>& img) :
     bf(newBf), bi(newBi), imgData(img) {}
 void Bmp::setImgData(const std::vector<uint8_t> newImgData) {
     imgData = newImgData;
 }