diff --git a/bmp/bmpinfoheader.cpp b/bmp/bmpinfoheader.cpp
--- a/bmp/bmpinfoheader.cpp
+++ b/bmp/bmpinfoheader.cpp
@@ -2,6 +2,10 @@
+BmpInfoHeader::BmpInfoHeader() : biSize(0), biWidth(0), biHeight(0), biPlanes(1), biBitCount(24), biCompression(0), biSizeImage(0), biXPelsPerMeter(0), biYPelsPerMeter(0), biClrUsed(0), biClrImportant(0) {
+    // Initialize member variables in the constructor
+    initMemberVars();
    biSize = sizeof(BmpInfoHeader);
    biWidth = NZwidth;
    biHeight = height;