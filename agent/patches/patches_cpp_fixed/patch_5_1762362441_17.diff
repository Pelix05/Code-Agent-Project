diff --git a/bmp/bmpinfoheader.cpp b/bmp/bmpinfoheader.cpp
--- a/bmp/bmpinfoheader.cpp
+++ b/bmp/bmpinfoheader.cpp
@@ -3,9 +3,14 @@
 #include "bmpinfoheader.h"
-BmpInfoHeader::BmpInfoHeader() {
+BmpInfoHeader::BmpInfoHeader(int NZwidth, int height) : biSize(0), biWidth(0), biHeight(0), biPlanes(1), biBitCount(24), biCompression(0), biSizeImage(0), biXPelsPerMeter(0), biYPelsPerMeter(0), biClrUsed(0), biClrImportant(0) {
     // Initialize member variables in the constructor
     biSize = sizeof(BmpInfoHeader);
     biWidth = NZwidth;
     biHeight = height;
+    // Initialize uninitMemberVar warnings
+    bfType = 0;
+    bfSize = 0;
+    bfReserved1 = 0;
+    bfReserved2 = 0;
+    bfOffBits = 0;
+    // Initialize remaining uninitialized member vars if needed
+    // (e.g., for BmpFileHeader)
+    // ...
 }