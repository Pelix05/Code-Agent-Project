diff --git a/bmp.cpp b/bmp.cpp
--- a/bmp.cpp
+++ b/bmp.cpp
@@ -10,7 +10,7 @@ Bmp::Bmp(BmpFileHeader &newBf, BmpInfoHeader &newBi, std::vector<uint8_t> img)
     // BmpFileHeader bf(NZwidth, height);
     // BmpInfoHeader bi(NZwidth, height);
     bf = newBf; // Use initialization list for performance
     bi = newBi; // Use initialization list for performance
     imgData = img; // Use initialization list for performance
@@ -15,7 +15,7 @@ Bmp::Bmp(BmpFileHeader &newBf, BmpInfoHeader &newBi, std::vector<uint8_t> img)
 }
 #include "bmpfileheader.h"
+#include "bmpinfoheader.h"
 class Bmp {
     BmpFileHeader bf;
     BmpInfoHeader bi;
     std::vector<uint8_t> imgData;