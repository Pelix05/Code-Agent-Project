diff --git a/bmp/bmpinfoheader.cpp b/bmp/bmpinfoheader.cpp
--- a/bmp/bmpinfoheader.cpp
+++ b/bmp/bmpinfoheader.cpp
@@ -1,12 +1,12 @@
-BmpInfoHeader::BmpInfoHeader()
+BmpInfoHeader::BmpInfoHeader(int NZwidth, int height) :
     biSize(0), biWidth(0), biHeight(0), biPlanes(1), biBitCount(24), biCompression(0), biSizeImage(0),
     biXPelsPerMeter(0), biYPelsPerMeter(0), biClrUsed(0), biClrImportant(0)
    biWidth = NZwidth;
    biHeight = height;
    // Initialize member variables here if needed