diff --git a/puzzle-challenge/labels.py b/puzzle-challenge/labels.py
--- a/puzzle-challenge/labels.py
+++ b/puzzle-challenge/labels.py
@@ -132,7 +132,7 @@
         self.disable_image,
         rendered["disable"])
        self.image = self.idle_image
-       self.clicked = False
-       self.hover = True
+       self.clicked = False
+       self.hover = False
    def render_text(self):
-       font_path, size = self.font, self.font_size
+       # Ensure font is defined before accessing it
+       if hasattr(self, 'font'):
+           font_path, size = self.font, self.font_size
+       else:
+           font_path, size = None, None
        key = (font_path, size)